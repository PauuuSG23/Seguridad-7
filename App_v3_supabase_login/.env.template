# Copie este archivo a `.env` y reemplaze los valores por los de su proyecto Supabase

# Claves de la app
FLASK_SECRET_KEY=pon_una_clave_larga_y_aleatoria
WTF_CSRF_SECRET_KEY=otra_clave_larga_y_aleatoria

# ReCAPTCHA (opcional si usa captchas)
RECAPTCHA_SITE_KEY=tu_recaptcha_site_key
RECAPTCHA_SECRET_KEY=tu_recaptcha_secret_key

# Conexión a la base de datos (SQLAlchemy)
# Formato: postgresql+psycopg://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgresql+psycopg://postgres:YOUR_DB_PASSWORD@db.YOUR_SUPABASE_HOST.supabase.co:5432/postgres

# Cadena DSN para psycopg (sin +psycopg)
# Formato: postgresql://<user>:<password>@<host>:<port>/<dbname>
PSYCOPG_DSN=postgresql://postgres:YOUR_DB_PASSWORD@db.YOUR_SUPABASE_HOST.supabase.co:5432/postgres

# Alternativamente (variables por separado)
DB_USER=postgres
DB_PASSWORD=YOUR_DB_PASSWORD
DB_HOST=db.YOUR_SUPABASE_HOST.supabase.co
DB_PORT=5432
DB_NAME=postgres

# Notas:
# - Supabase requiere SSL (en psycopg usar sslmode=require o connect_args={"sslmode":"require"}).
# - No suba `.env` con credenciales públicas a repositorios públicos. Mantenga estas variables fuera del control de versiones.
